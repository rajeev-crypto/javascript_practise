// A student learning js was trying to make  a function. His code should concatenate a passed string name 
// with string "rails" and store it in varibale called result. he need to help to fix this code 

function nameString(name){
    var b = "rails"
    var result = name+b
    return result 
    // return name + "rails"
}

console.log(nameString("rajeev"))