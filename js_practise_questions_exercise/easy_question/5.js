// Buggy code 

// Fix the code in the code tab to pass this challange(only syntex error). Look at the examples 
// below to get an idea of what the funcction should do.Buggy

function sumArray(arr){
    var sum = 0;
    for(i=0;i<arr.length;i++){
        sum+=arr[i];
    }
    return sum;
}'

'